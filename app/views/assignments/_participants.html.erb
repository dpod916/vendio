<%= simple_form_for(item, html: { class: 'form-horrizontal'}, remote: true)  do |f| %>
    <%= f.input :participant_tokens, as: :string, label: "Update Participants", input_html: {data: { pre: item.participants }} %>
    <%= f.button :submit,  "Update", {class: 'btn btn-xs btn-white pull-left', id: 'add-button'} %>
<% end %>
<%= link_to "Cancel", item, {class: 'btn btn-xs btn-white pull-left', id: 'participant-cancel-button', remote: true } %>

<% content_for :javascript do %>

<script type="text/javascript">

$("#participant-cancel-button").click(function() {

            $('*[id*=_form]').each(function() {
                $(this).hide();
            });

            $('*[id*=s_button]').each(function() {
                $(this).show();
            });

            $('.token-input-list-facebook').remove();



});


</script>

<% end %>