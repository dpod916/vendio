$('#div_id').html("<%= escape_javascript(render (partial: 'comments/form', locals: {commentable: comment.commentable, comment: Comment.new, placeholder: 'Reply to comment', button_class: 'btn-sm btn-primary', button: 'Reply', rows: 10, parent: comment.id }).html_safe) %>")

